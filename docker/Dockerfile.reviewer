FROM node:20-alpine

WORKDIR /app

RUN corepack enable && corepack prepare pnpm@latest --activate

COPY reviewer/package.json reviewer/pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile || pnpm install

COPY reviewer/ .

EXPOSE 5175

CMD ["pnpm", "dev"]
