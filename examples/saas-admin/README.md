# SaaS Admin Panel

A subscription management and customer support admin panel for SaaS products. Demonstrates enum badges, currency fields, date filters, and bulk actions.

## Quick Start

```bash
go run ./cmd/serve --spec examples/saas-admin/spec.yaml --data examples/saas-admin/data.json
```
