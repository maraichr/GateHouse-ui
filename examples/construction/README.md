# Construction Management Portal

A subcontractor management and compliance tracking application for construction companies. Demonstrates the full complexity of GateHouse UI: state machines with guards, relationship tables, stepped forms, dashboards with stat cards, and role-based permissions.

## Quick Start

```bash
go run ./cmd/serve --spec examples/construction/spec.yaml --data examples/construction/data.json
```
